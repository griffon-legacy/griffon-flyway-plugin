DESCRIPTION
        flyway-clean

	Drops all (tables, views, procedures, triggers, ...) in the configured schemas.

SYNTAX
        flyway-clean [options]

OPTIONS
        --datasource
                Name of the datasource on which the command will be invoked.
                (defaults to default)