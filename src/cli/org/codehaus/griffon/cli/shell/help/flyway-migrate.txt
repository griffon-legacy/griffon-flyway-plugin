DESCRIPTION
        flyway-migrate

	Triggers the migration of the configured database to the latest version.

SYNTAX
        flyway-migrate [options]

OPTIONS
        --datasource
                Name of the datasource on which the command will be invoked.
                (defaults to default)