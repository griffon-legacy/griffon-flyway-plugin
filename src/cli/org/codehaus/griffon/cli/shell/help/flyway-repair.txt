DESCRIPTION
        flyway-repair

	Repairs the metadata table after a failed migration on a database without
	DDL transactions.

SYNTAX
        flyway-repair [options]

OPTIONS
        --datasource
                Name of the datasource on which the command will be invoked.
                (defaults to default)